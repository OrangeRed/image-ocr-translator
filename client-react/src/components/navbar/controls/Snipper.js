import React from 'react'

function handleClick() {

  // acquire top-left coords and bottom-right coords
  // send coords into OCR library
  // send OCR result to server for API requests
  return;
}

function Snipper() {
  return (
    <button onClick={handleClick}>
      Snip
    </button>
  )
}

export default Snipper
