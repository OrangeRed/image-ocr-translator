import React from 'react'

function handleClick() {
  
  //show settings menu / div
  return;
}

function Settings() {
  return (
    <button onClick={handleClick}>
      Settings
    </button>
  )
}

export default Settings
